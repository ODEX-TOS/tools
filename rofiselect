#!/bin/bash

rofidir= "$HOME/bin/rofi"
prompt=$(ls $rofidir | awk "!/^askpass/" | sort)

sol=$(echo -e "$prompt" | rofi -dmenu -p 'Select subscript' -font 'Fira Code 10')

for i in $(ls $rofidir)
do
    if [ "$i" == "$sol" ];
    then
            eval "$rofidir/$i"
    fi
done
